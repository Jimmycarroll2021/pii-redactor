# Core library deps — required for the pipeline component
cryptography>=42.0.0

# HTTP client — used by LlamaCppClient and HFInferenceClient
requests>=2.31.0
huggingface_hub>=0.24.0

# FastAPI service
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0

# Gradio app (HF Space)
gradio>=4.36.0

# Tests
pytest>=8.0.0
