# Phase 3 Draft Plan: Scaled Runs and Reporting

## Objective

Execute repeatable scale runs and publish correctness, safety, and performance reports.

## Acceptance Criteria

- Run at least one 1k+ document direct library benchmark.
- Run at least two HTTP batch concurrency settings.
- Report latency percentiles, docs/sec, estimated docs/day, error rate, and category metrics.
- Assert no expected original PII values appear in safe outputs.
- Write a reproduction runbook.
